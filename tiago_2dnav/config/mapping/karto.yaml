slam_karto:
  base_frame: base_footprint

  # Scans:
  minimum_travel_distance: 0.3
  minimum_travel_heading : !degrees 11.5
  range_threshold        : 24.5
  use_scan_barycenter    : true
  resolution             : 0.025

  # ! As for now this feature works only with SICK lasers
  map_free_space         : false
